# 排除个人数据 / 运行时产物 — GitHub Release 和 git archive 自动忽略
cookies.txt export-ignore
skills/.eino/ export-ignore

# Shell scripts must use LF on checkout (bash on Linux)
*.sh text eol=lf
run.sh text eol=lf
upgrade.sh text eol=lf
